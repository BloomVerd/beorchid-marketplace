mutation Register($input: RegisterInput!) {
  register(input: $input) {
    accessToken
    refreshToken
    farmer {
      id
      email
      firstName
      lastName
      roles
      isFieldAgent
    }
  }
}
